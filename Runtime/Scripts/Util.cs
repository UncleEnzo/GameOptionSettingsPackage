namespace Nevelson.GameSettingOptions
{
    public static class Util
    {
        /// <summary>
        /// Converts the value fed in by the volume slider to logarithmic.
        /// which is what the mixer group uses
        /// </summary>
        /// <param name="volume"></param>
        /// <returns></returns>

    }
}